#!/bin/sh

if [ ! -f "/home/rufo/.config/chezmoi/key.txt" ]; then
    mkdir -p "/home/rufo/.config/chezmoi"
    chezmoi age decrypt --output "/home/rufo/.config/chezmoi/key.txt" --passphrase "{{ .chezmoi.sourceDir }}/key.txt.age"
    chmod 600 "/home/rufo/.config/chezmoi/key.txt"
fi
